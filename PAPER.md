# arneas — 基于线性自回归模型的图像压缩编码器

**ar-nearlossless-codec** · 逐像素自回归预测编码：一种极简参数下的近无损图像压缩方法

**An Autoregressive Prediction Codec for Near-Lossless Image Compression with Minimal Parameters**

> 摘要：图像压缩是在存储代价与画质保留之间取最优权衡的问题。本文沿一条独立推演出的思路——"相邻像素相似 → 预测 → 残差 → 量化 → 熵编码"——提出一种极简的三阶段压缩方法：先用最小二乘拟合一个**二维因果线性预测器**（三通道合计仅 18 个参数：绿/红/蓝分别 5/6/7 个），只对预测残差编码；再用均匀量化以步长 q 控制画质；最后按残差符号分布用算术编码逼近熵下界。本文推导并验证了均匀量化的失真模型 E[ε²]=q²/12 与画质上限 PSNRmax(q)=58.92−20·log₁₀(q)。在 24 张 Kodak 公开测试集镜像（缩至 500×500，见 data/kodak，可完整复现）上，本方法以 51.2 dB 近无损画质，平均码率低于同图 PNG 无损 **14.4%**（23/24 张低于 PNG）；相对未压缩位流压缩比约 6 倍，全尺寸原始影像压缩比 9.4 倍。文中完整披露了从一维窗口失败到二维窗口突破的思考与实验过程、以及条件熵模型、符号合并、游程编码、色度下采样等负结果实验，并给出与 JPEG-LS、CALIC、端到端深度压缩（Ballé 等, ICLR 2017）、单图过拟合（COIN, 2021）的定位对比。本方法的核心生态位是：单图自适应 + 极简参数 + 率失真环节可解析表达。

---

## 1. 引言

### 1.1 背景

一张照片动辄数 MB。压缩的目标是在"尽量少占空间"与"尽量保留画质"之间取得最优平衡——这是典型的约束优化问题。主流格式 JPEG 在低码率下产生明显的方块效应（block artifact），画质上限约为 41 dB；PNG 虽为无损格式，但对真实照片压缩率有限。一个自然的问题是：能否有一种方案，既达到人眼几乎无法察觉差别的"近无损"画质，又比 PNG 更省空间？

### 1.2 本文方法概览

本文方法的形成过程本身是本文内容的一部分——它不是照搬任何既有框架，而是沿一条自我推演的思路逐步搭建的：

1. **观察**：图像相邻像素高度相关（物体连片、颜色平滑），直接逐像素存储浪费巨大；
2. **第一步（预测）**：用已知像素"猜"未存像素，只存猜不准的部分——预测编码；
3. **第二步（量化）**：预测残差是浮点数，直接存代价大——均匀量化控制精度；
4. **第三步（熵编码）**：量化残差仍可进一步收紧——按符号分布用算术编码逼近信息熵下界。

这三步排序本身就是递减"意外"的发现：每做完一步，都会自然冒出下一个"该怎么存"的问题。整个过程在查阅任何图像压缩标准/文献之前独立完成（回溯检索见 §7）。

---

## 2. 模型假设与符号说明

### 2.1 基本假设

| 编号 | 假设 | 依据 |
|---|---|---|
| (A1) 因果性 | 图像按"左→右、上→下"逐像素重建，只用已重建邻居预测，不依赖未来像素 | 保证编解码两端一致、无需额外传信息 |
| (A2) 均匀量化 | 残差按步长 q 量化，量化误差在 [−q/2, q/2] 近似均匀分布 | 使得误差的期望方差可解析计算（§4.2） |
| (A3) 统计平稳 | 用本图残差频率估计符号分布并做熵编码 | 逐图自适应、无需外部先验 |
| (A4) 独立训练 | 预测器仅 18 个参数，对单张图最小二乘过拟合，权重随图传输 | 权重数≪像素数，大图下开销可忽略 |

### 2.2 符号

| 符号 | 含义 | 符号 | 含义 |
|---|---|---|---|
| x | 像素真值（0–255） | x̂ | 重建值 |
| q | 量化步长（灰阶） | R | 码率（bit/pixel, bpp） |
| H | 信息熵（bit/符号） | MSE | 均方误差 |
| PSNR | 峰值信噪比 (dB) | ε | 量化误差 |

---

## 3. 方法一：二维因果关系捕捉（预测器）

### 3.1 为什么是"二维"（思考过程）

最初版本使用**一维窗口**：把图像逐行拉成一条长序列，预测第 i 个像素时用其左侧连续 32 个已解码像素做线性回归。直觉上这是"只看前面 32 个点"，但实验立刻暴露问题——**它对竖直方向的渐变毫无感知**：

```
    亮度 100  100  100  100   ← 上一行
         110  110  110  110   ← 当前行（“左边”全是 100，预测仍是100）
         120  120  120  120   ← 真实值 110/120，永远猜不中
```

一维窗口的输入几乎全是"同一行左侧的点"，天然看不见"上面"的信息，因此对垂直渐变、物体边界等结构失明。

**修正是自然的**：预测应利用*真实的二维邻居*。最终模型用四个已解码邻居（图 2-1）：左 L、左上 TL、上 T、右上 TR——这四个点在行主序解码过程中**保证已经解码**（因果性），且覆盖了人类视觉系统最敏感的邻域关系。

### 3.2 模型定义

对绿色通道：

```
x̂(i,j) = w1·L + w2·TL + w3·T + w4·TR + b        (1)
```

其中 L、TL、T、TR 为对应邻居的已解码值。权重 w=(w1,w2,w3,w4) 与偏置 b 由**最小二乘**确定：最小化整图残差平方和

```
argmin_w,b  Σ (x(i,j) − x̂(i,j))²               (2)
```

这是凸二次规划，闭式解为正规方程 w=(XᵀX)⁻¹Xᵀy；数值上以 Adam 梯度下降 300 步求解（数据量 10⁵~10⁶ 行，收敛稳定）。

### 3.3 多通道级联（色彩相关性）

RGB 三通道高度相关（平坦区域三通道数值几乎相等）。为此红、蓝通道在四个邻居之外，再拼入**同位置的其它已解码通道值**：

| 通道 | 输入 | 参数（含偏置） | 解码顺序 |
|---|---|---|---|
| 绿 G | 4 个邻居 | 5 | 先 |
| 红 R | 4 邻居 + G 同位置值 | 6 | 次 |
| 蓝 B | 4 邻居 + G、R 同位置值 | 7 | 后 |

解码顺序"绿→红→蓝"保证每一步条件因果可用（与 PixelCNN 处理 RGB 的级联方式一致）。三通道合计 **18 个参数**——这正是"极简"的核心：18 ≪ 一张图的像素数（10⁵~10⁶），模型捕获的是图像的结构规律而非记忆像素。

> 实测发现（意外收获）：旧一维法每通道 32 权重、共 99 参数、种子 96 像素；新二维法仅 18 参数、种子 48 像素——**更少的参数与更小的固定开销，反而换来更低的码率**。这体现了"用对信息比堆参数更重要"。

### 3.4 训练与种子

- 训练：随机采样窗口批（batch=256），Adam，lr=1e-2，300 步，MSE 目标；
- 种子：每个通道最前面的 K 个像素**原样保存**，作为解码起点（K=16）；它同时为前几行像素提供邻居值；
- 权重 8-bit 定点量化后随包传输；传输包 = 权重 + 种子 + 残差比特流，三者均随模型尺寸增长、与图像尺寸无关。

### 3.5 闭环一致性（最重要实现原则）

自回归是**反馈系统**：每一步预测依赖前一步的重建结果。编码端与解码端模型若有任何不一致（如编码用 float 权重、解码用 8-bit 权重），误差将沿整幅图像累积——实测中仅使 11.8 dB 崩坏到 46.9 dB 上限（见调试记录）。因此**编码端在计算残差之前，先把自己的权重量化为解码端将要使用的版本**，用"解码端同款模型"跑闭环。这是本项目最重要的工程教训，也是所有实验可信的基础。

---

## 4. 方法二：残差量化（失真模型）

### 4.1 量化定义

预测后，真正要保存的是残差 e = x − x̂。直接存浮点残差需大量位（8 bit/符号 起步），故均匀量化：

```
r = round(e / q)
x̂重建 = 预测 + r·q
```

量化误差 ε = x − x̂ 满足 |ε| ≤ q/2。

### 4.2 失真模型推导（关键数学）

由假设 (A2)（误差在 [−q/2, q/2] 近似均匀分布），其期望方差：

```
E[ε²] = (1/q) · ∫(ε²) dε （区间 [−q/2, q/2]） = q²/12      (3)
```

**量化引入的均方误差与 q² 成正比**。代入 PSNR 定义（信号峰值 255）：

```
PSNR = 10·log10(255² / MSE)
PSNRmax(q) = 10·log10(255² / (q²/12)) = 58.92 − 20·log10(q)   (4)
```

由此：q=2 → 52.9 dB；q=4 → 46.9 dB；q=8 → 40.9 dB。**q 每翻倍，画质约降 6.02 dB**——这是"用画质换空间"的连续调节杆，也是率失真的参数化来源。

---

## 5. 方法三：熵编码（码率模型）

### 5.1 信息论下界

量化残差符号 r 服从离散分布 p(r)。由香农信息论，编码这些符号的理论最小平均位数是信息熵：

```
H = − Σ p(r)·log2 p(r)           (5)
R = H/3  （bpp，每像素 3 通道）   (6)
```

### 5.2 实现

用**算术编码**逼近熵下界：它对偏态分布（大量小残差）非常高效。符号表由本图残差频率估计（假设 A3），随包传输。

- 实测：算术编码距熵下界 <0.5%（0.013 bpp），哈夫曼距熵下界 <1.5%；
- 码率—失真权衡：增大 q → 残差更集中于 0 → 熵 H 变小 → 码率下降，形成一维可调曲线 (R(q), D(q))。

---

## 6. 实验与结果

### 6.1 实验设置

- **数据集**：Kodak 公开测试集镜像 24 张（500×500，来自公开 GitHub 镜像仓库；原始官方版为 768×512，本实验使用镜像缩小版，文件见 `data/kodak/`，可完整复现）；
- **实现**：Python（numpy/torch/numba，约 1000 倍闭环加速，42 Mpx/s）；代码见 `graph_code/`；
- **口径**：bpp 为真实传输字节（量化权重 + 种子 + 算术码流 + 符号表）折算；PSNR 在原始 RGB 域计算；
- **说明**：Kodak 镜像为 500×500（非官方 768×512 原版），绝对码率随分辨率略有变化，但本方法相对 PNG 的增益比例不受影响；
- **确定性**：全部固定随机种子、可复现。

### 6.2 与 PNG 无损对比（近无损 51 dB 档）

| 指标 | PNG 无损 | 本方案 q=2 | 相对 PNG |
|---|---|---|---|
| 平均码率 | 4.602 bpp | **3.940 bpp** | **−14.4%** |
| 画质 | 无损 | **51.17 dB** | — |
| 24 张中低于 PNG | — | **23/24**（仅 kodim20 打平）| 95.8% |

（跑完 `python3 experiments/kodak_bench.py` 输出精确逐图值。kodim20 为强细节影像，本方案 q=2 与 PNG 恰好持平，其余 23 张全部低于 PNG。）

### 6.3 与 JPEG 对比

| | 码率 | 画质 |
|---|---|---|
| 本方案 q=2 | 3.940 bpp | 51.17 dB |
| 本方案 q=4 | 2.982 bpp | 46.37 dB |
| JPEG q95 | 1.173 bpp | ~40.5 dB |
| JPEG q70 | 0.429 bpp | ~35 dB |

JPEG 用远低码率换 40 dB 档画质；但 **JPEG 受块效应限制无法进入 46 dB 以上区间**——近无损区（≥51 dB）是本方法的独占生态位。

### 6.4 率失真扫描（Kodak 平均）

| q | 理论 PSNRmax | 实测 PSNR | 实测码率 |
|---|---|---|---|
| 2 | 52.90 | 51.17 | 3.940 |
| 4 | 46.88 | 46.37 | 2.982 |
| 8 | 40.86 | 40.83 | 2.091 |
| 16 | 34.84 | 35.10 | 1.324 |

（以上均为 Kodak 24 张实测平均，MSE 训练与 q 无关，模型只训练一次后对各档复用。）

实测与理论相差 0.2~1.5 dB 以内、总体吻合（个别档因离散量化效应略超上限）——验证了 §4 的量化噪声模型。

### 6.5 预测器结构：一维窗口 vs 二维窗口

将二维模型换回最初的一维窗口模型（仅用同一行左邻，其余训练与编码不变），在 Kodak 24 张上对比近无损（q=2）表现：

| 指标 | 一维窗口 | 二维窗口 | 改进 |
|---|---|---|---|
| 平均码率 (bpp) | 4.233 | 3.940 | **−6.9%** |
| 平均 PSNR (dB) | 51.17 | 51.17 | 持平 |

二维邻居信息使竖直渐变可预测，残差熵下降，码率降低 6.9% 而画质不变——预测器"看得懂的结构"越多，残差越低，这正是预测编码的本质：把可预测结构先"算掉"，熵编码只负责不可预测的部分。



### 6.5 全尺寸原始影像验证

另取一张未压缩的相机原始 TIF（4288×2848，36.6 MB 像素数据）直接压缩：51 dB 近无损下输出约 11.7 MB（2.56 bpp），相对未压缩位流节省 89.3%（压缩比 9.4 倍），相对同尺寸 PNG 无损（2.78 bpp）仍省 8.1%。**说明此前缩略图实验中"只赢一点点"是因为缩略 PNG 含缩放噪声、bpp 虚高；面对真正的原始数据，本方法的压缩优势更为充分。**

---

## 7. 回溯定位：与经典方案和前沿工作的对照

### 7.1 定位说明

第 1~6 节的建模均由独立推演得出，动手前未参考任何图像压缩标准或文献；实验结果定稿后，为弄清本方法在既有技术体系中的位置，补充做了检索定位。本节省略的经典与前沿工作**均为事后对照所用、未参与建模**。

### 7.2 与经典无损/近无损框架

**宏观流程吻合**：本方法"预测 → 残差量化 → 熵编码"流程与 JPEG-LS（ISO/IEC 14495-1, 1999）和 CALIC（Wu & Memon, 1997）等经典框架顶层一致，从侧面印证了技术路线的合理性。

**预测器范式本质差异**：

| | 经典方案（JPEG-LS / CALIC） | 本方法 |
|---|---|---|
| 预测规则 | 专家预先设计（MED / GAP）、固定、不随图变 | 最小二乘从当前图挖出，随图自适应 |
| 回答的问题 | "一般图像该怎么猜" | "这一张图像该怎么猜" |
| 范式 | 人工经验（自上而下） | 数据驱动（自下而上） |
| 代价 | 无额外传输 | 18 个参数随图传输（大图摊薄趋零） |

**相对不足**：单一线性回归对强边缘/复杂纹理的猜中率不及 MED 修正与 CALIC 的上下文切换；未设游程模式，平坦区效率低于 JPEG-LS 游程机制。

**相对优势**：极简参数下的全局自适应（对细节丰富的图预测更准）；"绿→红→蓝"因果级联把通道相关性也纳入最小二乘框架（经典方案多用固定色差变换）。

### 7.3 与机器学习压缩前沿

| 路线 | 代表 | 参数规模 | 与本方法对比 |
|---|---|---|---|
| 全局泛化型 | Ballé 等, *End-to-end Optimized Image Compression*, ICLR 2017 | 数百万 | 需大规模训练，算力极高 |
| 单图过拟合型 | COIN（Dupont 等, 2021）隐式神经表示 | 数千~上万 | 非线性强但参数多一个量级 |
| **本方法** | — | **18** | 不引入非线性/深层网络，预测器退化为封闭凸优化（唯一全局最优），率失真环节可解析表达 |

**独特生态位**：单图自适应 + 极简参数 + 可解析率失真——人工经验与深度学习黑箱之间的轻量结合点。

### 7.4 为什么拒绝泛化（设计决策）

本方法从一开始就不做任何跨图像泛化训练，仅针对单图拟合。理由：

1. **分布外风险**：泛化依赖"训练集与测试集同分布"假设，而应用面对单张来源未知图像，分布外样本会导致预测偏差、残差增大、码率失控，且难以预判；
2. **数学确定性**：单图最小二乘是凸优化、闭式解唯一且全局最优——"训练集即测试集"，无需调参、不必担心过拟合/欠拟合的博弈；
3. **资源约束**：作为个人项目，不具备大规模数据与高算力条件；单图拟合仅需普通 CPU、秒级~分钟级收敛。

18 个参数不是粗糙近似，而是在上述约束下的自然选择。

---

## 8. 负结果与经验教训

建模中并非所有尝试都成功。如实记录负结果，能划清边界、避免后人重复弯路。

### 8.1 条件熵模型（上下文分桶）

- 设想：给残差分上下文统计、用更贴近的分布编码，预期省 15%~25%；
- 实测：仅省 1%~5%；
- 原因：线性预测后残差已接近随机噪声，与局部上下文的互信息仅约 0.05~0.15 bit，可再提取的结构信息本就不多。

### 8.2 符号域优化（DP 合并 / 码长感知训练）

- DP 符号合并、码长感知训练合计收益不足 5%；
- 原因：该架构下最小化均方误差与最小化熵近似同向，符号编码空间已近耗尽。

### 8.3 游程编码（RLE）

- 设想：对连续零残差做游程编码可省位；
- 实测：残差中零段平均长度仅 1.3~3.0，凑不出"长串零"，游程反而略增开销。

### 8.4 YCbCr 与 4:2:0 色度下采样

- 可省 30%~50% 码率；
- 但色度量化误差经变换放大进 RGB，画质下降 4~12 dB；
- 与"近无损"定位冲突，故弃用（码率收益数据保留参考）。

### 8.5 综合经验

**残差熵是码率的主体**；把它压向信息论下限的符号域手段（熵模型、符号合并、游程）收益已封顶。真正显著的改进来自**让预测器本身更准**——窗口由一维改二维，Kodak 24 张平均码率下降 6.9%（见 §6.5）。后续应着力于更强、更自适应的预测建模，而非符号编码的单点优化。

---

## 9. 局限与展望

1. **平坦/渐变区**：对大片天空、白墙类内容，PNG 游程压缩更优。改进方向：在残差流中加入游程模式（RUN）；
2. **预测器为线性**：对强边缘/复杂纹理表达能力有限。改进：在二维特征基础上引入 MLP 或更大邻域，参数仍少的同时进一步压残差熵；
3. **训练时间**：逐图训练+顺序解码，超大图偏慢。实测单张 500×500 图，一维窗口压缩耗时 30.0 s，二维窗口仅 5.6 s（提速 5.3 倍）；闭环部分经 numba 编译已较纯 Python 快约 1000 倍（42 Mpx/s）。对比单图过拟合型学习压缩（COIN 单图需训练 5 万次迭代，本方法每通道仅 300 步、共 900 步），本方法训练量低两个数量级。可进一步使用"训练共享"（MSE 训练与 q 无关，多档扫描只训一次）；
4. **失真指标**：PSNR 对空间细节不敏感，可引入感知指标（SSIM/MS-SSIM）。

---

## 10. 结论

本文提出一种极简参数的近无损图像压缩方法：二维因果线性预测（18 参数）→ 均匀量化 → 算术编码。在 Kodak 公开测试集上，51 dB 近无损画质下平均码率低于 PNG 无损 14.4%（23/24 张低于 PNG）；相对未压缩位流压缩比约 6 倍，全尺寸原始影像 9.4 倍。方法推导了可解析的量化失真模型并实验验证；完整披露了思考过程、失败实验与定位对比。其核心意义在于：**压缩应按信息量（熵）而非像素个数分配存储**——预测器负责把结构信息先编码掉，量化按可容忍误差控制尺度，熵编码负责逼近信息论极限。

---


## 参考文献

[1] Zhang A, Lipton Z C, Li M, Smola A J. 动手学深度学习[M/OL]. 何孝霆 等 译. 北京: 人民邮电出版社, 2020. https://zh.d2l.ai. （作者实际阅读的教材，本文思路启发来源）

[2] Weinberger M J, Seroussi G, Sapiro G. The LOCO-I lossless image compression algorithm: principles and standardization into JPEG-LS[J]. IEEE Transactions on Image Processing, 9(8), 2000: 1309-1324. （回溯检索，定位对照：JPEG-LS）

[3] Wu X, Memon N. Context-based, adaptive, lossless image coding[J]. IEEE Transactions on Communications, 45(4), 1997: 437-444. （回溯检索，定位对照：CALIC）

[4] Ballé J, Laparra V, Simoncelli E P. End-to-end Optimized Image Compression[C]. International Conference on Learning Representations (ICLR), 2017. （回溯检索，生态位对比：全局泛化型学习压缩）

[5] Dupont E, Goliński A, Alizadeh M, Teh Y W, Doucet A. COIN: COmpression with Implicit Neural representations[C/OL]. arXiv:2103.03123, 2021. （回溯检索，生态位对比：单图过拟合型学习压缩）

[6] van den Oord A, Kalchbrenner N, Espeholt L, Kavukcuoglu K, Vinyals O, et al. Conditional Image Generation with PixelCNN Decoders[C]. Advances in Neural Information Processing Systems (NIPS), 2016. （回溯检索，级联通道建模思路对照）

> 引用说明：正文中所有点名提及的文献均已列于此。[1] 为作者实际阅读的教材；[2]–[6] 为实验定稿后回溯检索所得，仅用于定位对照，均未参与建模。

---
## 附录 A：复现指引

```bash
# 环境
pip install numpy torch numba pillow

# Kodak 基准（24 张，约 8 分钟）
python3 experiments/kodak_bench.py

# 单张图压缩（命令行）
cd graph_code
python3 main.py compress ../data/kodak/kodim01.png out.npz --K 16 --q 2 --steps 300 --window 2d
python3 main.py decompress out.npz out.png

# 全尺寸原始影像验证（需自备 TIF 原图）
python3 experiments/full_res_test.py <your.tif>
```

## 附录 B：文件结构

```
├── README.md            # 项目首页
├── PAPER.md             # 本文（完整论文）
├── LICENSE              # 代码：CC BY-NC-SA 4.0；论文：CC BY-NC-ND 4.0
├── NOTICE.md            # 使用与引用声明
├── graph_code/          # 核心代码
│   ├── codec.py         #   预测器/闭环/压缩解压（1d/2d 窗口）
│   ├── entropy_codec.py #   哈夫曼/算术熵编码 + 训练共享
│   ├── final_bench.py   #   统一基准
│   └── main.py          #   CLI
├── experiments/         # 实验脚本
│   ├── kodak_bench.py   #   Kodak 基准（本论文数据来源）
│   ├── exp_2d_window.py #   2D 窗口实验
│   ├── exp_cond_entropy.py / cond_entropy.py   # 条件熵（负结果）
│   ├── exp_merge.py / exp_entropy.py           # 符号合并/码长感知（负结果）
│   ├── exp_ycbcr.py     #   色度下采样（负结果）
│   └── fast_loop_bench.py / fast_arith_decode.py  # 提速实验
├── data/kodak/          # Kodak 公开测试集镜像（24 张，500×500）
└── assets/              # 论文图表
```

---

> **仓库**：本方法完整代码、实验数据与论文公开于 GitHub 仓库 [**arneas**](https://github.com/IjalG/arneas)（ar-nearlossless-codec · 基于线性自回归模型的图像压缩编码器），可复现本文全部数字。

## 附言（研究志）

本文附上一点研究过程的个人记录：完成这篇工作最有趣的不是最终结果，而是路线本身——由"图像为什么这么大"这个朴素疑问出发，一步步自己推导出"预测→量化→熵编码"，事后才发现这与学界沉淀多年的 JPEG-LS 框架不谋而合。过程中大量失败实验（条件熵、符号合并、游程、色度下采样）教会我最重要的判断：**坏的尝试也是信息**——它们给"该往哪里使劲"划出了边界。以及，除非有海量算力与数据支撑，对单张图"量体裁衣"地在近无损场景里，就是最优解。